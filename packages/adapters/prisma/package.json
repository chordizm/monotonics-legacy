{
  "name": "@monotonics/adapter_prisma",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "migrate:sqlite": "prisma migrate dev --name=sqlite --schema=schema/sqlite.prisma"
  },
  "devDependencies": {
    "@monotonics/core": "*",
    "@prisma/client": "^4.14.1",
    "prisma": "^4.14.1"
  }
}
